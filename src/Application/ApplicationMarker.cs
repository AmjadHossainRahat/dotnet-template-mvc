namespace Application
{
    /// <summary>
    /// Marker class to reference the Application assembly (useful for reflection / DI registration).
    /// </summary>
    public sealed class ApplicationMarker
    {
    }
}
